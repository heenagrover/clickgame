import React from "react";
import "./Header.css";

const Header = () => (
   <header className="header">
   	<h2>Click on an image and remember not to click on it more than once!</h2>
   </header>
);
export default Header;