import React from "react";
import "./Header.css";

const Header = () => (
   <header className="header">
   	<h2>Click on an image but don't click on any more than once!</h2>
   </header>
);
export default Header;